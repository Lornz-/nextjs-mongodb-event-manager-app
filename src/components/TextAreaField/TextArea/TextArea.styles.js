import styled from 'styled-components';

import { defaultStyle } from '@/components/TextInputField/TextInput/TextInput.styles';

const StyledTextArea = styled.textarea`
  ${defaultStyle}

  height: 100%;
`;

export default StyledTextArea;
